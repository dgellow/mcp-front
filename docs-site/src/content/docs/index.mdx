---
title: MCP Front
description: The missing auth layer for MCP servers
template: splash
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import AnimatedHero from '../../components/AnimatedHero.astro';

<AnimatedHero 
  title="MCP Front" 
  tagline="OAuth 2.1 proxy for Model Context Protocol servers" 
/>

## Your MCP servers are naked

Seriously. You're running MCP servers with bearer tokens hardcoded in Claude's config. Your database credentials are sitting in plaintext. Your internal APIs are one leaked config away from the internet.

MCP Front wraps your servers in proper OAuth 2.1 auth. Real security, not security theater.

<CardGrid stagger>
  <Card title="OAuth 2.1 + PKCE">
    Modern auth. Not your 2015 OAuth flow.
  </Card>
  <Card title="JWT tokens">
    HS256. Configurable TTL. No cookies.
  </Card>
  <Card title="Firestore">
    Persistent storage. Or memory mode for dev.
  </Card>
  <Card title="Go performance">
    Sub-millisecond proxy overhead. Promise.
  </Card>
</CardGrid>

## How it actually works

<div class="architecture-diagram">
  <object data="/mcp-front/architecture.svg" type="image/svg+xml" aria-label="MCP Front Architecture">
    <img src="/mcp-front/architecture.svg" alt="MCP Front Architecture" />
  </object>
</div>

1. Claude tries to connect to your MCP server
2. Gets redirected to OAuth flow (Google by default, because of course)
3. User proves they're not a rando
4. mcp-front proxies the connection to your actual MCP server
5. Your database credentials stay secret, your job stays secure

## Why you need this

### Bearer tokens are not auth
If your security model is "hope nobody finds this string", you're doing it wrong. OAuth 2.1 or GTFO.

### Google knows who you are
Integrates with Google Workspace. Domain restrictions mean `sketchy@gmail.com` can't access your prod database.

### It just works™
Docker image, binary, whatever. Deploys to Cloud Run in 2 minutes. Scales to infinity\*.

<small>\*Infinity not guaranteed. Please consult your SRE before attempting infinity.</small>

### Protocol translation for free
Your stdio-based MCP server becomes an SSE endpoint. Magic? No, just good engineering.

## Start in 30 seconds

```bash
# Clone it
git clone https://github.com/dgellow/mcp-front
cd mcp-front

# Configure it (copy and edit)
cp config-oauth.example.json config.json

# Run it
docker-compose up -d
```

That's it. You now have auth. You're welcome.

## What people actually use this for

<CardGrid>
  <Card title="Postgres without tears" icon="database">
    Let Claude query your database without giving it your master password like a fool
  </Card>
  <Card title="Git but secure" icon="git-branch">
    Clone private repos without storing GitHub tokens in Claude's brain
  </Card>
  <Card title="Notion for robots" icon="document">
    Your company wiki, now accessible to AI, without the IT department having a stroke
  </Card>
  <Card title="Whatever you want" icon="puzzle">
    Got an API? Slap mcp-front in front. Boom, it's secure now.
  </Card>
</CardGrid>

## Now what?

<CardGrid>
  <Card title="Just start" icon="rocket">
    [Getting started guide](/mcp-front/getting-started/) - 5 minutes to working auth
  </Card>
  <Card title="Configure stuff" icon="setting">
    [Configuration docs](/mcp-front/config/overview/) - Make it work for your setup
  </Card>
  <Card title="OAuth deep dive" icon="shield">
    [OAuth guide](/mcp-front/oauth/overview/) - For when you need to impress security
  </Card>
  <Card title="Ship it" icon="cloud">
    [Deploy to prod](/mcp-front/deployment/production/) - Because localhost isn't a deployment strategy
  </Card>
</CardGrid>
